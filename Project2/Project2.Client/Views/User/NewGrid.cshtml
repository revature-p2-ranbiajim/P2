<head>
  <title>New Grid</title>
  <link rel="stylesheet" href="~/css/site.css" />
</head>

@model Project2.Client.Models.GridViewModel

@{
    ViewBag.Title = "New Grid";
}

<body>
    <table>
      <th id="table">
        <tr>
          <td>
            <form action="/user/usermenu" method="get">
              <button class="boton" type="submit">Back to Menu Without Saving</button>
            </form>
          </td>
          <td></td>
          <td></td>
          <td></td>
          <td></td>
          <td></td>
          <td></td>
          <td></td>
          <td>
            <form action="/user/savegrid" method="post" onsubmit="@Model.SaveGrid = exportGrid()">
              <button class="boton" type="submit">Save for Later and Go Back to Menu</button>
            </form>
          </td>
        </tr>
      </th>
    </table>
    <br>
    

    <br>
    <div class="gridButtons">
      <input id="color-text" placeholder="Enter a color (000000)">
      <button class="boton1" onclick="changeColor()">Change Color</button> @* <--- change this out of hardcode! *@
      <button class="boton1" onclick="undoLast()">Undo</button>@* <--- change this out of hardcode! *@
      <button class="boton1" onclick="saveGridAsPNG()">Export to PNG</button>
      @* <button onclick="exportGrid()">Save For Later</button> @* <--- change this out of hardcode! *@
    </div>
    <br>
    <br>
    
    <canvas id="backgroundGrid" width="500" height="500" style="position: absolute; left: 200: top: 0; z-index: 0;">
        Grid not supported.
    </canvas>
    <canvas id="myCanvas" width="500" height="500" style="position: absolute; left: 200: top: 0; z-index: 1;">
        Grid not supported.
    </canvas>
    <script type="text/javascript">
      let chosenWidth = '@(Model.Width)';
      let chosenHeight = '@(Model.Height)';
      let gridName = '@(Model.Name)';
    </script>
    <script src=site.js> </script>>
</body>